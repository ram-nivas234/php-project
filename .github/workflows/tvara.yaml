name: Auto Merge Tvara to Main

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Git
        run: |
          git config user.name "ram-nivas234"
          git config user.email "github-actions@users.noreply.github.com"

      - name: Merge Tvara into main
        run: |
          git fetch origin main
          git checkout main
          git merge origin/Tvara --no-edit
          git push origin main
